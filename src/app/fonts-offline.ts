// Offline font configuration for testing in sandboxed environments
// This provides fallback fonts when Google Fonts are not accessible

export const roboto = {
  className: 'font-sans',
  style: { fontFamily: 'system-ui, -apple-system, sans-serif' },
  variable: '--font-roboto',
};

export const bebas = {
  className: 'font-sans',
  style: { fontFamily: 'system-ui, -apple-system, sans-serif' },
  variable: '--font-bebas',
};

export const badscript = {
  className: 'font-sans',
  style: { fontFamily: 'system-ui, -apple-system, sans-serif' },
  variable: '--font-badscript',
};

export const dosis = {
  className: 'font-sans',
  style: { fontFamily: 'system-ui, -apple-system, sans-serif' },
  variable: '--font-dosis',
};

export const slab = {
  className: 'font-serif',
  style: { fontFamily: 'Georgia, serif' },
  variable: '--font-slab',
};